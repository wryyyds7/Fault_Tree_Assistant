spring:
  application:
    name: document-ingest-service
  config:
    import: optional:nacos:${spring.application.name}.yaml?namespace=fta-namespace&group=DEFAULT_GROUP&extension=yaml
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: fta-namespace
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
        namespace: fta-namespace
        group: DEFAULT_GROUP
  rabbitmq:
    host: ${rabbitmq.host:localhost}
    port: ${rabbitmq.port:5672}
    username: ${rabbitmq.username:guest}
    password: ${rabbitmq.password:guest}
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 10
        default-requeue-rejected: false
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8091

# 文档存储配置
# 文件存储路径，默认使用用户主目录下的 fta-data/documents
# 可通过环境变量 DOCUMENT_STORAGE_PATH 或系统属性 project.root 自定义
document:
  storage:
    path: ${DOCUMENT_STORAGE_PATH:${user.home}/fta-data/documents}
  ocr:
    tessdata: ./tessdata
